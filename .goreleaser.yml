version: 2

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/mozza
    binary: mozza
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/gshepptech/mozza/internal/version.Version={{.Version}}
      - -X github.com/gshepptech/mozza/internal/version.Commit={{.Commit}}
      - -X github.com/gshepptech/mozza/internal/version.Date={{.Date}}

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: darwin
        format: zip

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"
